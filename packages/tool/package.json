{
  "name": "yiyi-tool",
  "version": "1.0.1",
  "description": "常用工具",
  "main": "index.js",
  "files": [
    "lib",
    "dist",
    "es"
  ],
  "scripts": {
    "build": "node ./build/build.js",
    "build:lib": "node ./build/build.js --format=cjs --out=lib",
    "build:es": "node ./build/build.js --format=esm --out=es",
    "build:dist": "node ./build/build.js --format=cjs --out=dist",
    "publish": "npm publish",
    "test": "node index.test.js"
  },
  "keywords": [
    "lib",
    "dist",
    "es",
    "src"
  ],
  "author": "yiyi",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.1.0",
    "esbuild": "^0.14.38",
    "yargs-parser": "^21.0.1"
  }
}
